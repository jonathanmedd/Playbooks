---
- name: Test API
  uri:
   url: "https://httpbin.org/delete"
   method: DELETE
   #user: "{{ temporaryCredentials.applianceUsername }}"
   #password: "{{ temporaryCredentials.password }}"
   #validate_certs: no
   #force_basic_auth: yes
   return_content: yes
   status_code:
   - 200

  register: apiStatusOutput