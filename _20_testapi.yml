---
- hosts: all
  gather_facts: false
  tasks:

  - name: Test API
    uri:
      url: "https://httpbin.org//basic-auth/test/Password"
      method: GET
      user: "test"
      password: "Passwo"
      #validate_certs: no
      force_basic_auth: yes
      return_content: yes
      status_code:
      - 200

    register: apiStatusOutput