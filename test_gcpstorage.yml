---
- hosts: all
  tasks:

  - name: Test Download
    gc_storage:
      gs_access_key: "{{ gcp_access_key }}"
      gs_secret_key: "{{ gcp_secret_key }}"
      bucket: "test290506"
      object: "{{ item }}"
      dest: "/opt/binaries/{{ item }}"
      mode: "get"
    with_items:
      - "test.txt"
      - "powershell-yaml/"
    # become: yes
